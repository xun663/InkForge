package com.inkforge.memory;

/** Category of a character fact — drives merge and update semantics. */
public enum FactCategory {
    IDENTITY, ABILITY, AFFILIATION, POSSESSION, STATE,
    RELATIONSHIP, APPEARANCE, PERSONALITY, OTHER
}
